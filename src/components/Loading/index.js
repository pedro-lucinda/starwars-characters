import React from "react";
import ReactLoading from "react-loading";

const Loading = () => {
  return <ReactLoading type={"cylon"} color="#fff" height={25} width={50} />;
};

export default Loading;
